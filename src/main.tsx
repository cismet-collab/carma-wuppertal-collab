import React from "react";
import ReactDOM from "react-dom/client";



ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>
      ... add here components which make problems when debugging in storybook
    </div>
  </React.StrictMode>
);
